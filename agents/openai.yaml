interface:
  display_name: "Grill Lite"
  short_description: "Batch critical decisions, then move to delivery"
  default_prompt: "Use $grill-lite to surface only the decisions that block this request, then route it to the lightest useful next step."

policy:
  allow_implicit_invocation: false
