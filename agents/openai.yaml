interface:
  display_name: "Grill Lite"
  short_description: "Batch blockers, then use a lightweight route"
  default_prompt: "Use $grill-lite to settle only the decisions that block this request, then continue through direct execution or a lightweight prototype, spec, or ticket set."

policy:
  allow_implicit_invocation: false
