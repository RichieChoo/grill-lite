interface:
  display_name: "Grill Lite"
  short_description: "Clarifies key decisions and makes delivery easy to follow"
  default_prompt: "Use $grill-lite to inspect first, batch high-value clarification, confirm shared understanding, and complete this request with proportionate rigor."

policy:
  allow_implicit_invocation: false
